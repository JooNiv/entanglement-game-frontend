services:
  frontend:
    build:
      context: ./
      dockerfile: Dockerfile
    container_name: acf-demo-frontend
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5173:5173"
