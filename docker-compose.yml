services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: acf-demo-frontend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    ports:
      - "5173:5173"
    depends_on:
      - backend
